🐛 BUG: Unknown column 'UpdateAt' in 'field list'
================================================

ISSUE:
------
Click "Xác Nhận Thanh Toán" → Error 500: Unknown column 'UpdateAt'

ROOT CAUSE:
-----------
Backend trying to update column 'UpdateAt' but it doesn't exist in DB

QUICK FIX:
----------
Backend team needs to do ONE of the following:

1. CHECK DATABASE SCHEMA:
   DESCRIBE Payout_Requests;
   
2. IF 'UpdateAt' doesn't exist, check what column name is used:
   - UpdatedAt (with 'd')
   - updated_at (snake_case)
   - LastUpdated
   - Or add the column

3. FIX BACKEND CODE:
   Find: PATCH /admin/payout-requests/:id/approve
   
   Change SQL from:
   UPDATE Payout_Requests SET UpdateAt = NOW(), Status = 'paid'
   
   To (if DB has UpdatedAt):
   UPDATE Payout_Requests SET UpdatedAt = NOW(), Status = 'paid'
   
   To (if DB has updated_at):
   UPDATE Payout_Requests SET updated_at = NOW(), status = 'paid'

FRONTEND:
---------
✅ Frontend code is correct - no changes needed
✅ API call is correct: PATCH /admin/payout-requests/{id}/approve

FILES:
------
- Frontend: src/models/admin.api.ts (Line 360-369)
- Frontend: src/pages/admin/AdminPayoutRequestsPage.tsx (Line 78-92)
- Backend: Find payout approve endpoint and fix SQL

STATUS:
-------
🔴 BLOCKED - Waiting for backend fix
Priority: HIGH

